{% extends 'base.html.twig' %}

{% block title %}{{ 'jobeet.homepage.title'|trans }}{% endblock %}

{% block main %}
    {% import 'job/macro/list.html.twig' as jobList %}

    {% for category in categories %}
        {{ jobList.renderCategory(category) }}
        {{ jobList.renderList(category.jobs) }}
    {% endfor %}
{% endblock %}
